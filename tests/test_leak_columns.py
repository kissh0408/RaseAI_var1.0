"""Archived: model_training pipeline_common leak tests (Benter rebuild Phase 0)."""

from __future__ import annotations

import pytest

pytestmark = pytest.mark.skip(reason="model_training pipeline_common archived in Benter rebuild Phase 0")


def test_leak_columns_archived() -> None:
    """Placeholder so pytest collection succeeds without model_training imports."""
