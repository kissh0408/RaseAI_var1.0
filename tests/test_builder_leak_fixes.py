"""Archived: model_training builder leak tests (Benter rebuild Phase 0)."""

from __future__ import annotations

import pytest

pytestmark = pytest.mark.skip(reason="model_training builders archived in Benter rebuild Phase 0")


def test_builder_leak_fixes_archived() -> None:
    """Placeholder so pytest collection succeeds without model_training imports."""
